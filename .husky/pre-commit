#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

if [ "$SKIP_PRECOMMIT_BUILD" = "1" ]; then
  echo "[husky] Skipping bun run build (SKIP_PRECOMMIT_BUILD=1)"
  exit 0
fi

echo "[husky] Running bun run build..."
bun run build
